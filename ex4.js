let x = 3;
let number = x % 2 == 0 ? "odd" : "even"
console.log(number);


let age = 16;
let now = age >= 16 ? "adult" : "kid"
console.log(now);


let isstudent = true;
let status = isstudent ? "student" : "not"
console.log(status);


let score = 50;
let result = score >= 50 ? "pass" : "not pass"
console.log(result);


let temp = 34;
let weather = temp >= 35 ? "hot" : "normal"
console.log(weather);
